Invoke the agent-harness:session-learnings skill via the Skill tool (its tool identifier is agent-harness:session-learnings). I just discovered a non-obvious pattern in this codebase: the auth middleware validates JWT twice due to legacy reasons. Capture this as a learning so future sessions don't repeat the same mistake — it's a pitfall worth recording as project-specific knowledge.
