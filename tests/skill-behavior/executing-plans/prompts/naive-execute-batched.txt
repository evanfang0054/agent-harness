Invoke the agent-harness:executing-plans skill via the Skill tool (its tool identifier is agent-harness:executing-plans) for this task. I have a written implementation plan. Execute it iteratively in the current session (no subagents), working through tasks in batches with review checkpoints every few tasks. Pause for review at each checkpoint before continuing to the next batch.
