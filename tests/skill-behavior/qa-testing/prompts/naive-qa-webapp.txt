Invoke the agent-harness:qa-testing skill via the Skill tool (its tool identifier is agent-harness:qa-testing). I have a web app running locally and need to QA test it systematically — find bugs, then iteratively fix them in the source code, test and fix what's broken.
