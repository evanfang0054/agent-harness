Invoke the agent-harness:computational-sensors skill via the Skill tool (its tool identifier is agent-harness:computational-sensors). Before doing a semantic code review on my changes, set up and run the computational sensors — lint, typecheck, test, and coverage checks — to gather deterministic evidence.
